package tilo.compose.core.layers.vector

/**
 * Vector styling is authored through `VectorTileStyleConfigMap` and compiled by
 * `VectorTileStyleMapCompiler` into `VectorTileLayerStyle` runtime objects.
 */
internal object VectorTileStylingArchitecture
